# Hub
Flask>=2.0
flask-cors>=3.0
requests>=2.25
Pillow>=9.0
python-osc>=1.8    # QLab -> hub OSC bridge (optional; bridge auto-disables if absent)
zeroconf>=0.70     # hub-side mDNS discovery of nodes (optional; disables if absent)

# Nodes
# Flask (above) is reused by the node HTTP server.
# System packages (not pip): mpv, ffmpeg  (ffmpeg/ffprobe needed on the hub
# for video tiling; mpv needed on each display node).
