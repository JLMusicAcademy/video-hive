# Hub
Flask>=2.0
flask-cors>=3.0
requests>=2.25
Pillow>=9.0

# Nodes
# Flask (above) is reused by the node HTTP server.
# System packages (not pip): mpv, ffmpeg  (ffmpeg/ffprobe needed on the hub
# for video tiling; mpv needed on each display node).
